name: Issue metrics report

on:
  push:
  workflow_dispatch:
  schedule:
    - cron: 15 5 * * 1-5

permissions:
  issues: read
  pull-requests: read


jobs:
  issue-metrics-report:
    uses: George-Smirnoff/reusable-workflows/.github/workflows/RW_issue_metrics.yml@9dc4404c55558fe812ae8b32c87b73d6668a6c32
    with:
      repository_path: ${{ github.repository }}